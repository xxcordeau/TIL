# 프로그래머스 매일 풀이

매일 하나씩 프로그래머스 문제를 풀어서 기록하는 폴더입니다.

- `queue.json` : 풀 문제 목록과 진행 상황을 관리하는 파일
- `solutions/` : 실제로 푼 코드가 쌓이는 폴더 (파일명: `날짜-문제ID-제목.js`)

풀이는 순서대로 진행하고, 다 풀면 `solved`를 `true`로 바꾸고 `solvedDate`를 채워 넣습니다.
